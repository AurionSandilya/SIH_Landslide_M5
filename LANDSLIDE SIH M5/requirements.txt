# Pinned to the versions this fix was implemented and tested against
# (Python 3.12, PostgreSQL 16). No requirements.txt existed in the
# repository before this fix - it was one of the audit's flagged missing
# "dependency files".
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
sqlalchemy==2.0.52
asyncpg==0.31.0
apscheduler==3.11.3
httpx==0.28.1
python-multipart==0.0.20
